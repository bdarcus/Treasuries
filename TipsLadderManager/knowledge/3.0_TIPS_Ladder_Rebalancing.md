# 4.0 TIPS Ladder Rebalancing

## Dependencies

**Requires:** 3.0 TIPS Ladders

**Inherits all terms from 3.0 TIPS Ladders:**

- [DARA](../../shared/knowledge/GLOSSARY.md#dara), [ARA](../../shared/knowledge/GLOSSARY.md#ara), [Gap Years](../../shared/knowledge/GLOSSARY.md#gap-years), Synthetic TIPS construction
- [P+I per TIPS](../../shared/knowledge/GLOSSARY.md#pi-per-tips), Construction Method (longest to shortest)

**Inherits all formulas from 2.1 TIPS Basics:**

- [indexRatio](../../shared/knowledge/GLOSSARY.md#index-ratio), [parValue](../../shared/knowledge/GLOSSARY.md#par-value-adjusted), adjustedAnnualInterest
- lastYearInterest, [piPerBond](../../shared/knowledge/GLOSSARY.md#pi-per-tips)

**Inherits all terms from 2.0 Bond Ladders:**

- [Funded Year (FY)](../../shared/knowledge/GLOSSARY.md#funded-year), [Later Maturity Interest](../../shared/knowledge/GLOSSARY.md#lmi)

---

## Purpose

Rebalance an existing TIPS ladder to align with a desired amount target (DARA). Two modes:
- **Gap**: Minimal trades; sells excess bracket TIPS to fund newly available maturities (formerly gaps).
- **Full**: Rebuilds the entire ladder from the current portfolio's total cash value, resetting to a clean DARA target.

## Context

TIPS ladders require periodic rebalancing as new TIPS are issued and gaps are filled. Originally, gaps (years with no TIPS) were covered by additional TIPS in bracket years. As new TIPS are issued, rebalancing sells this additional amount to buy the new maturities. Additionally, market price changes may drift the ladder's income profile; a full rebalance resets the entire ladder to a clean DARA target using the current portfolio value.

---

## Rebalancing-Specific Concepts

### Official Ladder Range

- **firstYear**: Earliest year with holdings
- **lastYear**: Latest contiguous year with holdings before any missing rung above 2040.
- Missing rungs above 2040 truncate the official range (e.g., holdings in 2047 and 2049 but not 2048 → lastYear = 2047)
- Known gap years (2037-2039, no TIPS issued) do NOT break contiguity if 2040 is held — **mechanism**: if 2040 is held, the range unconditionally extends through the gap years.
- **Non-contiguous TIPS > 2040**: Contribute annual interest to earlier years but are NOT counted as rungs for DARA calculation or ladder range determination unless `lastYearOverride` extends the range to include them.
- **`lastYearOverride`** (optional UI parameter): When provided, replaces the derived `lastYear`. Allows the user to declare the intended last funded year, which may be a gap year (e.g., 2039) or an extended year (e.g., 2042). The UI infers a sensible default and shows a dropdown for manual adjustment.
  - **Shortening**: When `lastYearOverride < derivedLastYear`, all holdings in years `(lastYearOverride, derivedLastYear]` that were contiguous with the original ladder are sold in full (produce sell rows in output). These years are excluded from the rebuild.
  - **Extending**: When `lastYearOverride > derivedLastYear`, years in `(derivedLastYear, lastYearOverride]` with no current holdings are treated as rebalance years — the appropriate TIPS for those years are identified from market data and buy rows are generated.

### lastYear as a Gap Year

When `lastYearOverride` is a gap year (i.e., `lastYear ∈ gapYears`), the upper bracket year (2040) is **not a funded rung** — all its holdings are treated as bracket excess:

- `fundedYearQty(2040) = 0` in Phase 3 and Phase 4 (`yearDara(2040) = 0`)
- `excessQtyBefore(2040) = currentHoldings(2040)` (all current TIPS are excess)
- Phase 2 (`calculateGapParameters`): 2040's LMI contribution uses **actual current holdings qty** (not a target computed from DARA) since no funded-year target exists for 2040.
- `rungCount` = `lastYear − firstYear + 1` (gap years count as rungs; their income is funded by bracket excess)

**UI inference for default lastYear**: After loading holdings, if the last maturity is 2040, `lastYear` defaults to 2040 (or the last gap year, 2039, if the gap ends immediately before). If the last maturity is before 2040, that year is used. The dropdown includes all available TIPS years to allow extension.

### RefCPI Date Override

By default, RefCPI is looked up for the settlement date. The UI allows overriding the reference date (click the RefCPI value in the info strip). When overridden:
- All P+I, cost, and ARA calculations use the RefCPI for the selected date instead of the settlement date
- Auto-inferred DARA (if previously computed) is cleared — rerun to refresh
- The info strip shows the override date in grey next to the RefCPI value

### DARA

DARA is entered by the user in the web UI (optional — leave blank to auto-infer). An inferred DARA is also computed from current holdings for validation:

1. Build a temporary TIPS ladder based on holdings, with no special handling for gap years; e.g., all interest from 2040 holdings (FY and excess) contributes to later maturity interest for funded years 2036 and lower
2. Sum ARA for all years in ladder range. numRungs for this calc is lastYear - firstYear + 1
3. Inferred DARA = total ARA / numRungs
4. Both values displayed in summary output for comparison

### Per-Year DARA (`daraByYear`)

An optional `Map<fundedYear, number>` allowing each funded year to target a different annual real amount. When provided:

- Each year in Phase 4 uses `daraByYear.get(year) ?? DARA` as its target — years absent from the map fall back to the scalar DARA
- Gap years always use the scalar DARA (they have no TIPS and do not appear in the UI panel)
- Bracket target funded-year qty is computed with the bracket year's own DARA from the map
- When `daraByYear` is null/absent, behaviour is identical to the scalar-DARA path

**UI trigger**: entering a DARA value opens a per-year panel (in the output area) pre-filled with the entered value for each funded year. Clearing the DARA field hides the panel and restores the scalar path. The panel year list is derived from holdings (rebalance) or firstYear–lastYear × tipsMap (build), making it forward-compatible with all available TIPS years.

### Current Gap Years

Years within the official ladder range where **no TIPS exist in market data AND no holdings exist** for that year. Currently 2037, 2038, 2039.

### Bracket Years

- **Upper bracket**: Always 2040 (Feb maturity)
### Bracket Identification Rules

1.  **CUSIP Aggregation**: When identifying the bracket with the most holdings, the total quantity of each CUSIP must be summed across the entire portfolio (all accounts and all maturity years).
2.  **Excess ARA Priority**: The primary identification metric is **Excess ARA** (`araByYear[y] - DARA`). The lower bracket is identified as the CUSIP in the year with the greatest positive difference between its current ARA and the target DARA. This ensures TIPS with the most "spare" capacity for duration matching are prioritized.
3.  **Selection Range**: The lower bracket must be selected from holdings maturing between `LOWEST_LOWER_BRACKET_YEAR` and the first gap year (exclusive).
4.  **Tie-Breaking**: Among candidates with equal Excess ARA, pick the single CUSIP with the highest aggregate quantity.

### Rebalance Years

Years that will receive buys/sells during rebalancing. Depends on method:

- **Gap-only**: Years between the lower bracket year and the first current gap year, exclusive on both ends. These were gap years when the ladder was originally built but now have TIPS available.
- **Full**: All years in [firstYear, lastYear] that are neither bracket years nor gap years. **Includes years without current holdings** if they fall within the range (e.g., when extending the ladder to 2041+).

### Funded Year vs Excess (Bracket Years)

- **Funded year portion**: Holdings needed to achieve DARA for that year's funded rung
- **Excess portion**: Additional holdings beyond the funded year target, held for duration matching gap years
- **Target Funded Year Qty**: For bracket maturity, calculated using residual P+I after non-bracket maturities in that year are settled
- Calculations use P+I for funded year / ARA, cost for duration matching
- **Gap-only**: non-bracket CUSIPs in bracket year held constant; bracket CUSIP sized to residual
- **Full**: non-bracket CUSIPs in bracket year subject to sell-earliest-first logic (same as non-bracket years); bracket CUSIP sized to remaining residual; only bracket CUSIP can be bought

---

## Named Quantities & Formulas

**Gap Total Cost** (`gapTotalCost`):
```
Gap Total Cost = Σ [ qty_y × 1,000 ]
  where qty_y = ROUND((DARA − LMI_y) / (PI_y / 1,000))
```
Theoretical cost of the gap years TIPS if they were issued today. Each gap year's required quantity (`qty_y`) is rounded to the nearest integer, with 1 = 1,000 of face value. Synthetic TIPS are assumed to be issued at face value (price 100, Index Ratio 1.0), so each integer quantity costs exactly 1,000.

**Funded Year Quantity** (`fyQty`, `fundedYearQty` in build-lib):
Target quantity for the funded-year rung — how many TIPS needed to produce DARA from this year's P+I plus later maturity interest.

**Excess Quantity Before** (`excessQtyBefore`):
```
excessQtyBefore = max(0, qtyBefore - fyQty)
```
Current holdings beyond the funded year target. Represents prior-build excess for duration matching. Clamped to 0: in 3-bracket mode the new lower bracket was previously a regular rebalance rung, so its fyQty can exceed current holdings.

**Excess Quantity After** (`excessQtyAfter`):
```
General (new lower, upper):
  excessQtyAfter = ROUND(targetExcess_cost / costPerTips)
    where targetExcess_cost = gapTotalCost × bracketWeight
          costPerTips = price/100 × indexRatio × 1,000

3-bracket original lower only:
  excessQtyAfter = max(0, qtyBefore - fyQty)   (= excessQtyBefore, preserved exactly)
```
Post-rebalance excess for duration matching. The general formula converts the theoretical cost target to quantity using the inflation-adjusted cost per bond.

**Exception — 3-bracket original lower bracket**: `bracketExcessTargetCost` is stored in real (uninflated) cost, but the general formula divides by nominal (inflation-adjusted) `costPerTips`. The ratio `realCost / nominalCost = 1/indexRatio`, so the formula would under-count bonds by `1/indexRatio`, producing a spurious net sell. For the original lower the intent is zero net trading (w1 is anchored at current excess), so `excessQtyAfter` is set directly to `excessQtyBefore`.

**Gap Amount** (`excessAmt` in build-lib; computed inline in rebalance popup):
```
gapAmount = excessQty × piPerTips
  where piPerTips = Par × (1 + coupon/2 × nPeriods)
               = Par + lastYearInterest   (equivalent forms)
```
Note: `piPerBond = Par × (1 + coupon/2 × nPeriods)` is the code form of the spec formula `Par + lastYearInterest` — algebraically identical.

**Gap Cost** (`excessCost` in build-lib; computed inline in rebalance popup):
```
gapCost = excessQty × costPerTips
```
(Note: This is nominal cost for cash flow reporting; internal allocation uses real cost).

**Cost Before** (bracket row, funded year portion):
```
costBefore_FY = fyQty × costPerTips
```

**Cost After** (bracket row, funded year portion):
```
costAfter_FY = fyQty × costPerTips
```
(Funded year quantity is unchanged before/after; all cost delta is in excess portion.)

**Cost Before/After** (non-bracket row):
```
costBefore = qtyBefore × costPerTips
costAfter  = qtyAfter  × costPerTips
```

**Cash Delta** (`cashDelta`, `costDelta`):
```
cashDelta = -(qtyDelta × costPerTips)
  where qtyDelta = qtyAfter - qtyBefore
```
Positive = cash received (net sell); negative = cash outflow (net buy).
Nominal cost is used here as it represents actual cash movement.

**Amount Before / Amount After** (ARA Before/After — `araBeforeTotal`, `araAfterTotal`):
See Phase 5 below for full component formulas.

---

## Rebalance Modes

### Gap-Only (existing)
Rebalance only years between lower bracket and first gap year (exclusive). All other non-bracket years use current qty. DARA is user-specified. Algorithm: Phases 1–5 below.

### Full Rebalance — Build-From-Cash (new)
**Concept:** Treat full rebalance as "sell everything, rebuild from scratch." The target is a fresh ladder built with the inferred DARA at the current portfolio's total cash value. Trades are current holdings vs target.

**Algorithm:**
1. Compute `portfolioCash` = sum of (current qty × costPerBond) for all holdings
2. Derive `firstYear` / `lastYear` from holdings (Phase 1 logic below)
3. Binary search DARA: call `runBuildFromScratch(dara, lastYear)` → get `totalBuyCost` → converge until `totalBuyCost ≤ portfolioCash` using the largest possible integer DARA.
4. Target = build output at converged DARA
5. Trades: for each year, `qtyDelta = targetQty − currentQtyForThatCUSIP`; any current CUSIPs not in target are sold in full

**Key properties:**
- Net cash ≥ 0 by construction (build cost ≤ portfolio cash)
- DARA is always inferred from cash — not user-specified for full rebalance
- Gap years / brackets / duration matching: identical to `runBuildFromScratch` (build-lib.js)
- Non-contiguous rungs (> lastYear): included in portfolioCash but not in the rebuilt ladder (future enhancement)

---

## 3-Bracket Mode

**Concept:** When anchorBefore (the latest 10-year TIPS below the first gap year) is a rebalance year (formerly a gap year, now available), optionally use three brackets — original lower, new lower, upper — for gap duration coverage. Gap avg duration falls between the new lower and upper durations, making new lower the natural duration partner for upper. Original lower retains legacy excess from when new lower did not exist.

**Weights formula (normal case — w1 ≤ 1):**
```
w1     = currentExcessOrigLower_cost / gapTotalCost   ← fixed; orig lower excess not bought or sold
w2_raw = (gapAvgDuration − d_upper + w1×(d_upper − d_origLower)) / (d_newLower − d_upper)
w2     = max(0, w2_raw)   ← clamp: if orig lower alone overshoots duration target, skip new lower
w3     = 1 − w1 − w2
targetExcess_cost: origLower = w1 × gapTotalCost (unchanged); newLower = w2 × gapTotalCost; upper = w3 × gapTotalCost
```
(Unadjusted cost used for `w1` to match `gapTotalCost`).

**Phase 4 behavior (normal case):**
- Original lower: excess held exactly as-is (never bought, never sold)
- New lower: buy to targetExcess_cost; if w2=0 (orig lower overshoots duration), no new lower excess bought
- Upper: buy or sell to targetExcess_cost as usual

**When w2_raw < 0 (but w1 ≤ 1):** Orig lower overshoots the duration target. New lower is skipped (w2=0); upper absorbs the remainder (w3 = 1 − w1 > 0). Duration match is approximate.

**2-bracket fallback (w1 > 1):** Orig lower excess alone exceeds the full gap cost. In this case the "freeze orig lower" rule breaks down — there is no valid 3-bracket solution. Fall back to standard 2-bracket using orig lower as the lower bracket:
```
origLowerWeight = (d_upper − gapAvgDuration) / (d_upper − d_origLower)   ← 2-bracket formula
newLowerWeight3 = 0
upperWeight3    = 1 − origLowerWeight
```
Orig lower excess is sold down to its 2-bracket target. No new lower excess bought. Summary flag: `bracketFellBack3to2 = true`.

**UI toggle:** "2-bracket" vs "3-bracket (original lower + new lower + upper)". New lower bracket CUSIP/year displayed when 3-bracket selected.

---

## Algorithm Flow (Gap-Only)

### Summary

- **Phase 1 — Derive Parameters:** Fetch settlement date and RefCPI, load holdings, determine ladder range (firstYear/lastYear), identify current gap years, compute inferred DARA
- **Phase 2 — Gap Parameters:** For each current gap year, interpolate yield and compute synthetic coupon and duration. Outputs Gap Avg Duration and Gap Total Cost used for bracket sizing
- **Phase 3 — Bracket Identification & Weights:** Identify lower/upper bracket CUSIPs, compute their durations and weights. Optionally compute before-state excess cost for display/verification.
- **Phase 4 — Ladder Rebuild (longest to shortest):** Standard ladder build using post-rebalance quantities throughout. ALL years in [firstYear, lastYear] processed longest to shortest (including brackets). Bracket years hold targetFYQty + excess per duration weights. Later maturity interest at each step is the running sum from already-processed longer years only.
- **Phase 5 — Verification Output:** Build Before/After ARA table, trades table, weight verification, net cash. Write HTML and CSV

---

### Phase 1: Derive Parameters

```
1a. Settlement date:
    Determined by script—walk back from today until FedInvest returns price data

1b. RefCPI:
    Fetched from TreasuryDirect secindex endpoint for the settlement date

1c. DARA:
    Input parameter (CLI --dara flag or interactive prompt)

1d. Build holdings ladder:
    Process all holdings longest to shortest
    Calculate ARA for every funded year
    Include annual interest from non-contiguous TIPS > 2040

1e. Determine official range:
    firstYear = first year with holdings
    lastYear = last contiguous year before any missing rung above 2040
    Non-contiguous TIPS > 2040 do NOT extend lastYear

1f. Calculate inferred DARA:
    SUM(ARA for funded years in range) / count of funded years in range
    Display alongside user DARA for validation

1g. Identify gap years:
    Years in [firstYear, lastYear] where no TIPS exist in market data AND no holdings exist
```

### Phase 2: Gap Parameters

Calculate synthetic TIPS for gap years (e.g., 2037-2039 as of Feb 2026):

- **Anchors**: anchorBefore = TIPS maturing in **January** of (minGapYear − 1); anchorAfter = earliest TIPS maturing in **February** of any year strictly after maxGapYear. Currently Jan 2036 and Feb 2040. `anchorAfter` searches for the first Feb TIPS with year > maxGapYear (not necessarily maxGapYear+1, since the next year may also be a gap year when lastYearOverride truncates the gap).
- Interpolate yields linearly between anchorBefore.yield and anchorAfter.yield using calendar time (maturity dates)
- Set synthetic maturity to **February 15** of the gap year
- Calculate synthetic coupon: MAX(0.00125, FLOOR(yield × 100 / 0.125) × 0.00125)
- Calculate MDURATION for each synthetic using the synthetic maturity and yield
- **PI per TIPS for gap year qty**: `1,000 × (1 + syntheticCoupon / 2)`.
- Gap Avg Duration = average of synthetic durations
- **Gap Total Cost**: Theoretical cost of the gap years TIPS if they were issued today.
  ```
  Gap Total Cost = Σ [ qty_y × 1,000 ]
    where qty_y = ROUND((DARA − LMI_y) / (PI_y / 1,000))
  ```
  This calculation determines the exact aggregate cost of the gap block before matching durations.
- Use target qty for 2040 (not actual holdings) and actual holdings for 2041+
- DO NOT add synthetic gap year interest back to the gap later maturity interest pool

### Phase 3: Bracket Identification and Weights

```
3a. Identify brackets:
    Upper: 2040 (always Feb maturity)
    Lower: Among holdings in [LOWEST_LOWER_BRACKET_YEAR, firstGapYear),
           pick the single CUSIP with most holdings (by qty)

3b. Calculate MDURATION for each bracket maturity

3c. Weights (2-bracket mode):
    lowerWeight = (upperDuration - gapAvgDuration) / (upperDuration - lowerDuration)
    upperWeight = 1 - lowerWeight
    targetExcess_cost per bracket = gapTotalCost × bracketWeight

3c-alt. Weights (3-bracket mode): see ## 3-Bracket Mode section above for formula.
    origLowerWeight
    newLowerWeight
    upperWeight
    targetExcess_cost per bracket = gapTotalCost × bracketWeight

3d. Before-state display only (not used in rebuild):
    Cost per TIPS = price/100 × indexRatio × 1000
        Cost per TIPS Unadjusted = price/100 × 1000
        Current Excess Unadjusted cost = (currentQty - targetFYQty_before) × costPerTIPSUnadjusted
      where targetFYQty_before uses current-holdings later maturity interest
    Before weights = currentExcess_cost / totalCurrentExcess_cost for each bracket
```

### Phase 4: Ladder Rebuild (longest to shortest)

Process ALL years in [firstYear, lastYear] **longest to shortest**, including bracket years. Gap years are skipped (no TIPS exist). Maintain a running `laterMatInt` pool — the sum of annual interest from all already-processed (longer-maturity) years using their post-rebalance quantities.

```
Initialize: laterMatInt = 0  (accumulates as each year is processed)

For each year from lastYear down to firstYear (skip gap years):
  1. Later maturity interest:
     - Rebalance years: use running laterMatInt (post-rebalance contributions from longer years)
     - Bracket years: use BEFORE-state LMI (sum of araLaterMaturityInterestByYear for years > current)
       Rationale: bracket funded-year qty is unchanged before/after (spec §Named Quantities);
       using the running rebuild LMI would let newly-bought bracket bonds reduce earlier bracket
       funded-year targets, causing spurious sells of existing bracket holdings.
     yearDara = daraByYear.get(year) ?? DARA   (per-year target, falls back to scalar DARA)
     residual = yearDara - lmiForYear  (what this year's own TIPS must cover)

  2. Determine post-rebalance qty per CUSIP:
     - If year has holdings: use latest-maturing CUSIP in that year as target.
     - If year has NO holdings (e.g. extension to 2041): pick latest maturity for this year from market data (tipsMap).
     - (Method-specific logic for non-target sells/buys follows same principles as Build or Gap-Only)
     - For bracket years: postQty = fyQty + excessQtyAfter (see §Named Quantities).
       For 3-bracket orig lower specifically, excessQtyAfter = excessQtyBefore (no net trade on orig lower target CUSIP).
```

### Phase 5: Verification Output

**Main table columns:**

- CUSIP, Qty, Maturity, Funded Year, Principal, Later Maturity Interest, ARA, Cost
- Target Qty, Qty Delta, Target Cost, Cost Delta
- ARA (Before), ARA-DARA Before, ARA (After), ARA-DARA After

**Sign conventions:**

- Qty Delta: negative = sell, positive = buy
- Cost Delta: positive = cash received (sell), negative = cash outflow (buy)
- Net Cash = SUM(Cost Delta): positive = excess cash, negative = need to add funds

**ARA columns:**

- ARA: Current holdings ARA (includes excess bracket TIPS at current qty)
- **ARA (Before)**: Ladder cashflow excluding excess bracket TIPS. Computed for the last-in-year CUSIP only:
```
  principalPerTips = 1,000 × indexRatio
  nPeriods         = 1 if month < 7, else 2
  ownPrincipal     = fyQty_before × principalPerTips
  ownCoupon        = fyQty_before × principalPerTips × coupon/2 × nPeriods
  laterMatInt      = araLaterMaturityInterestByYear[fy]
  ARA Before       = ownPrincipal + ownCoupon + laterMatInt
```
  where fyQty_before uses current-holdings later maturity interest (Phase 1 pool).

- **ARA (After)**: Post-rebalance ARA. Same formula using post-rebalance quantities:
```
  ownPrincipal  = fyQty_after × principalPerTips
  ownCoupon     = fyQty_after × principalPerTips × coupon/2 × nPeriods
  laterMatInt   = yearLaterMatIntSnapshot[fy]  (Phase 4 running pool)
  ARA After     = ownPrincipal + ownCoupon + laterMatInt
```
  For bracket years: fyQty_after = targetFYQty (not postRebalQty — excess excluded from own-rung ARA).
  Expected: ARA After ≈ DARA (within 1 rounding).

- **ARA-DARA**: Gap between ARA and target — minimizing this is an overarching goal

**Summary table:**

- Parameters (settlement date, refCPI, DARA, inferred DARA, firstYear, lastYear, rungs)
- Gap parameters (avg duration, total cost)
- Bracket details (duration, current/post-rebal/targetFY/excess quantities and costs)
- Weight verification: Target vs Before vs After
- Rebalance years list
- Net Cash

---

## Weight Verification

```
Before weights: current excess cost / total current excess cost for each bracket
After weights: post-rebal excess cost / total post-rebal excess cost for each bracket
Target weights: from duration matching formula (Step 4)

Expected: After weights ≈ Target weights (minor rounding differences)
Before weights typically show lower bracket overweighted (insufficient rebalancing since build)
```

## Excess Balance Check *(planned, not yet implemented)*

Validates whether brackets were properly funded for the full gap coverage task. Three components:

```
1. Bracket Excess Cost (gapExcess):
   For each bracket: (currentQty - targetFYQty) × costPerTips
   Sum across both brackets

2. Cost to Fill Rebalance Years (costForPrevGaps):
   For each rebalance year: targetFY cost - current holdings cost
   Sum across all rebalance years

3. Gap Theoretical Cost (gapCost):
   Sum of synthetic gap year quantities × 1,000
   (from Phase 2 calculation)

Surplus/(Shortfall) = 1 - 2 - 3

Near-zero: Brackets were properly funded for full gap coverage
Large negative: Brackets underfunded—insufficient excess to cover both
  rebalance year buys and theoretical gap year needs
Large positive: Brackets overfunded
```

**Note:** Gap Total Cost can be higher than expected when years above 2040 are underfunded relative to DARA (less later maturity interest → more gap TIPS needed → higher gap cost).

---

## Critical Principles

1. **ALWAYS process longest to shortest maturity** for later maturity interest calculations
2. **Later maturity interest** from years STRICTLY GREATER than current year
3. **Bracket maturity selection**: CUSIP with most holdings is the bracket CUSIP. Gap-only: non-bracket CUSIPs held constant. Full: non-bracket CUSIPs subject to sell-earliest-first; only bracket CUSIP can be bought
4. **Gap-only rebalance years**: always > lower bracket year and < first gap year (exclusive both ends)
5. **Full rebalance years**: all non-gap years in official ladder range (including brackets). Multi-CUSIP years use sell-earliest-first to minimize trades; buys only of latest CUSIP (or bracket CUSIP for bracket years)
6. **Post-rebalance quantities throughout Phase 4**: Later maturity interest at each step uses only the running pool built from already-processed longer years — never current (pre-rebalance) holdings. There is no separate "current holdings baseline + delta" cascade; the pool is built fresh longest to shortest using post-rebalance qtys.
7. **Gap year synthetic interest**: DO NOT add back to gap later maturity interest pool during Phase 1 calculation
8. **P+I for funded year sizing, cost for duration matching**: Two different units for two different purposes
9. **Bracket year own-rung ARA**: For a bracket year's own funded rung, use targetFYQty for P+I (not postRebalQty). The excess TIPS serve gap duration matching, not that rung's funded amount. The full postRebalQty annual interest does cascade to all earlier years.
10. **Non-contiguous TIPS > 2040**: Contribute interest to earlier years but NOT counted as rungs for DARA or lastYear determination
11. **No paired sell-then-buy in one funded year (FULL mode)**: Use FLOOR (not CEIL) for non-target sell qty. CEIL can over-sell, driving curPI below residual and causing a spurious compensating buy of the target CUSIP. FLOOR keeps curPI ≥ residual after each non-target sell, preventing this paired trade pattern.

---

## Configuration & Variables

See the [Technical Reference](./TECHNICAL_REFERENCE.md) for variable mappings and project constants (e.g., `REFCPI_CUSIP`).

## Key Terms

For all foundational definitions and formulas, refer to the [Master Glossary](../../shared/knowledge/GLOSSARY.md).
